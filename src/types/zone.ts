// Re-exported from schemaValidatation — zone domain types/schemas live there
// per architecture: schemaValidatation/ holds all API DTOs mirroring backend models
export * from "@/schemaValidatation/zone";
